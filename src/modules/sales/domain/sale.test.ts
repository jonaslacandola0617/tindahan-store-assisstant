import{describe,expect,it}from"vitest";import{confirmSaleInput,mergeSaleLines,saleTotals,shouldIgnoreRapidScan}from"./sale";
describe("sales domain",()=>{it("accepts only positive whole quantities",()=>{expect(confirmSaleInput.safeParse({lines:[{productId:"p1",quantity:1}],idempotencyKey:"sale-key-1"}).success).toBe(true);expect(confirmSaleInput.safeParse({lines:[{productId:"p1",quantity:0}],idempotencyKey:"sale-key-1"}).success).toBe(false);expect(confirmSaleInput.safeParse({lines:[{productId:"p1",quantity:1.5}],idempotencyKey:"sale-key-1"}).success).toBe(false)});it("merges duplicate draft lines deterministically",()=>{expect(mergeSaleLines([{productId:"b",quantity:1},{productId:"a",quantity:2},{productId:"b",quantity:3}])).toEqual([{productId:"a",quantity:2},{productId:"b",quantity:4}])});it("suppresses only rapid duplicate scan reads",()=>{expect(shouldIgnoreRapidScan({code:"4800016640017",at:1000},"4800016640017",1800)).toBe(true);expect(shouldIgnoreRapidScan({code:"4800016640017",at:1000},"4800016640017",2000)).toBe(false);expect(shouldIgnoreRapidScan({code:"4800016640017",at:1000},"2800000000068",1100)).toBe(false)});it("calculates unit and monetary totals",()=>{expect(saleTotals([{quantity:2,unitPrice:8},{quantity:3,unitPrice:12.5}])).toEqual({quantity:5,amount:53.5})})});
