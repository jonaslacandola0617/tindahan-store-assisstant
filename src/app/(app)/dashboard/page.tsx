import Link from"next/link";import{cookies}from"next/headers";import{getServerSession}from"next-auth";import{redirect}from"next/navigation";import{Icon}from"@/components/icon";import{authOptions}from"@/modules/identity/infrastructure/auth-options";import{dashboardSales}from"@/modules/sales/application/sales-service";import{searchProducts}from"@/modules/inventory/application/inventory-service";
const money=(value:string)=>new Intl.NumberFormat("en-PH",{style:"currency",currency:"PHP"}).format(Number(value));
export default async function DashboardPage(){const session=await getServerSession(authOptions);if(!session?.user.id)redirect("/sign-in");const locale=(await cookies()).get("tindahan-language")?.value==="FIL"?"FIL":"EN";const fil=locale==="FIL";const[sales,inventory]=await Promise.all([dashboardSales(session.user.id),searchProducts(session.user.id,{limit:1})]);const date=new Intl.DateTimeFormat(fil?"fil-PH":"en-PH",{weekday:"long",month:"long",day:"numeric",timeZone:"Asia/Manila"}).format(new Date());const firstName=session.user.name?.split(" ")[0]??(fil?"kaibigan":"there");return <><div className="content-header"><h1>{fil?`Magandang araw, ${firstName}`:`Good day, ${firstName}`}</h1><p className="text-muted">{fil?`Narito ang nangyayari sa tindahan mo ngayong ${date}.`:`Here's what's happening at your store today, ${date}.`}</p></div><section className="section"><div className="section-header"><h2 className="section-heading"><Icon name="alert"/>{fil?"Kailangan ng iyong pansin":"Needs your attention"}</h2></div>{inventory.counts.low+inventory.counts.out>0?<div className="card card-tint-cream card-pad">{inventory.counts.out>0&&<Attention tone="danger" title={fil?`${inventory.counts.out} produktong ubos na`:`${inventory.counts.out} products are out of stock`} action={fil?"Tingnan ang imbentaryo":"Review inventory"}/>} {inventory.counts.low>0&&<Attention tone="warning" title={fil?`${inventory.counts.low} produktong paubos na`:`${inventory.counts.low} products are almost out`} action={fil?"Tingnan ang imbentaryo":"Review inventory"}/>}</div>:<div className="card empty-state dashboard-calm-state"><span className="empty-icon"><Icon name="check"/></span><h3>{fil?"Walang kailangang asikasuhin ngayon":"Nothing needs attention right now"}</h3><p className="empty-body">{fil?"Lalabas dito ang mga produktong paubos o ubos na.":"Low and out-of-stock products will appear here."}</p></div>}</section><section className="section"><div className="section-header"><h2 className="section-heading">{fil?"Buod ngayong araw":"Today's summary"}</h2><Link className="section-link" href="/sales">{fil?"Tingnan ang mga benta →":"See sales →"}</Link></div><div className="card card-pad"><div className="grid-3"><Stat value={money(sales.summary.amount)} label={fil?"Kabuuang benta":"Total sales"}/><Stat value={String(sales.summary.quantity)} label={fil?"Naibentang item":"Items sold"}/><Stat value={String(sales.summary.count)} label={fil?"Naitalang benta":"Sales recorded"}/></div></div></section><section className="section"><div className="section-header"><h2 className="section-heading">{fil?"Mabilisang gawain":"Quick actions"}</h2></div><div className="card card-tint-brand card-pad"><div className="quick-actions"><Quick href="/receipt-upload" icon="camera" label={fil?"I-scan ang resibo":"Scan receipt"}/><Quick href="/sales/new" icon="bag" label={fil?"Itala ang benta":"Record sale"}/><Quick href="/inventory" icon="package" label={fil?"Magdagdag ng stock":"Add inventory"}/><Quick href="/search" icon="search" label={fil?"Maghanap ng produkto":"Find product"}/></div></div></section><section className="section"><div className="section-header"><h2 className="section-heading">{fil?"Kamakailang aktibidad":"Recent activity"}</h2><Link className="section-link" href="/sales">{fil?"Tingnan lahat →":"View all sales →"}</Link></div>{sales.recent.length?<div className="row-list">{sales.recent.map(sale=><Link className="row-item" href={`/sales/${sale.id}`} key={sale.id}><span className="attn-icon info"><Icon name="bag"/></span><span className="row-main"><span className="row-title">{sale.status==="CORRECTED"?(fil?"Itinamang benta":"Sale corrected"):(fil?`Naitalang benta · ${sale.totalQuantity} item`:`Sale recorded · ${sale.totalQuantity} items`)}</span><span className="row-meta">{new Intl.DateTimeFormat(fil?"fil-PH":"en-PH",{dateStyle:"medium",timeStyle:"short",timeZone:"Asia/Manila"}).format(new Date(sale.confirmedAt??0))}</span></span><span className="row-value">{money(sale.totalAmount)}</span></Link>)}</div>:<div className="card empty-state dashboard-calm-state"><span className="empty-icon"><Icon name="bag"/></span><p className="empty-body">{fil?"Lalabas dito ang mga benta pagkatapos maitala.":"Recorded sales will appear here."}</p></div>}</section><section className="section"><div className="section-header"><h2 className="section-heading">{fil?"Kapaki-pakinabang na impormasyon":"Helpful insights"}</h2></div><div className="card card-tint-olive card-pad"><span className="badge badge-olive">{fil?"Kalagayan ng stock":"Stock check"}</span><p className="font-medium dashboard-insight">{inventory.counts.low+inventory.counts.out>0?(fil?`${inventory.counts.low+inventory.counts.out} produkto ang nangangailangan ng pansin.`:`${inventory.counts.low+inventory.counts.out} products need attention.`):(fil?"Maayos ang kasalukuyang kalagayan ng stock.":"Current stock levels look clear.")}</p><Link className="section-link" href="/inventory">{fil?"Buksan ang imbentaryo →":"Open inventory →"}</Link></div></section></>}
function Attention({tone,title,action}:{tone:string;title:string;action:string}){return <div className="attn-item"><span className={`attn-icon ${tone}`}><Icon name="alert"/></span><span className="attn-body"><span className="attn-title">{title}</span></span><Link className="btn btn-secondary btn-sm" href="/inventory">{action}</Link></div>};function Stat({value,label}:{value:string;label:string}){return <div><div className="stat-row"><span className="stat-value">{value}</span></div><span className="stat-label">{label}</span></div>};function Quick({href,icon,label}:{href:string;icon:"camera"|"bag"|"package"|"search";label:string}){return <Link className="quick-action" href={href}><span className="quick-action-icon"><Icon name={icon}/></span><span className="quick-action-label">{label}</span></Link>}
